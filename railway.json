{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacksPlan": {
      "phases": {
        "setup": {
          "pkgs": ["openssl", "pkg-config", "openssl-dev"]
        }
      }
    }
  },
  "deploy": {
    "startCommand": "cargo run --release",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
